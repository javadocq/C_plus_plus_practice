### protected 멤버

- 기본 클래스에서 접근 가능
- 유도 클래스에서 접근 가능
- 기본 또는 유도 클래스의 객체로부터는 접근 불가능하다.
	-> 클래스 안에서는 가능하지만 객체에서 ->으로 직접 접근은 불가능하다.
	-> Private는 유도 클래스에서 접근이 불가능 -> private은 상속과 관계없이 무조건 (자신) 클래스 내부에만 접근 가능하다.
	-> protected는 상속이 이루어지는 private이다.