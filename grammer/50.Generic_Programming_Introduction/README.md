### Generic Programming 

- 간략한 정의 -> 타입에 관계없이 동작하는 "일반적인" 코드를 작성하는 방법
- 기존에는 add() 함수에 대해 타입에 따라 개별적으로 구현을 했어야 했다.
- 하지만 제네릭 프로그래밍을 사용하면, 하나의 add() 함수로 다양한 타입에 대해 동작하도록 구현할 수 있다.

- 제네릭 프로그래밍의 구현 방법
	- 매크로 사용 <- 주의가 필요하다
	- 함수 / 클래스 템플릿 사용
