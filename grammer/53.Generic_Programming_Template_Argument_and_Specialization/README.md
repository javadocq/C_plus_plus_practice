### 템플릿의 다중 매개변수

- 서로 다른 이름을 사용하여 다중 매개변수를 정의할 수 있다.
- 매개변수가 다르다면 서로 타입이 다를 수도 있다.

```cpp
template <typename T1, typename T2>

T1 MAX(T1 a, T2 b) {
	return (a > b) ? a : b;
}
```

### 템플릿의 트굿화

- 특정 자료형에 대해서는 템플릿을 사용하지 않고 별도 구현한 함수를 사용하도록 명시 가능