### override 지정자

- 기본 클래스의 가상 함수는 오버라이드 가능하다.
- 오버라이드를 위해서는 함수의 원형과 반환형이 동일해야 한다.
- C++11에서는 override 지정자를 사용하여 오버라이드 여부를 명시적으로 나타낼 수 있다.

### final 지정자

- 클래스의 final
	-> 클래스를 더 이상 상속하지 못하도록 한다.
- 멤버 함수의 final
	-> 유도 클래스에서 가상 함수를 오버라이드 하지 못하도록 한다.